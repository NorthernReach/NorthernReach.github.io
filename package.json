{
  "name": "northernreach",
  "type": "module",
  "version": "1.0.0",
  "description": "Northern Reach — retro static site",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
